language: python
python:
    - "3.7"
sudo: false
install:
    - pip install -r requirements.txt
    - pip install -r dev_requirements.txt
    - npm install
    - bash -xe ./install-other-deps.sh
script:
    - ./run_tests_with_coverage.sh
after_success:
    - coveralls
